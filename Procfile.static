# 1. Build client assets, watching for changes.
# 2. Build server assets, watching for changes. Remove if not server rendering.
# 3. Run Rails server.
web: cd client/ && npm run build:server && npm run build:client && cd .. && rails s
