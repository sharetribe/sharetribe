- if @current_user && @current_user.has_admin_rights_in?(@current_community) && editing_allowed
  %p
    %a{:id => "edit_link", :href => "/editor" + request.path, :data => { :save_url => mercury_update_path(:content_type => content_type)  }}
      .icon-with-text-container
        = icon_tag("edit", ["icon-part"]) 
        .text-part= t("common.edit_page")
#page_content{:data => {:mercury => "full"}}
  = yield
