%tr
  %td{:align => "left", :style => "padding-top: 15px;"} 
    %font{body_font}
      = "#{@feedback.author_name_and_email} has sent the following feedback from #{service_name} community '#{@current_community.name}':"

= render :partial => "quote", :locals => { :quote => @feedback.content }

