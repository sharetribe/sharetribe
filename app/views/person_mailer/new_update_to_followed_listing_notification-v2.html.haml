- url = listing_url(@url_params.merge({:id => @listing.id}))
%tr
  %td.person-name
    %h1= t("emails.common.hi", name: PersonViewUtils.person_display_name_for_type(@recipient, 'first_name_only'))

%tr
  %td
    %p
      = t("emails.new_update_to_listing.has_updated_listing_you_follow_in_kassi", author: PersonViewUtils.person_display_name_for_type(@listing.author, "first_name_only"), listing: @listing.title)

= render :partial => "action_button-v2", :locals => { :text => t("emails.new_update_to_listing.view_changes"), :url => url}
