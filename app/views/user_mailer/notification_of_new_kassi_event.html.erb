<% other_party = @kassi_event.get_other_party(@recipient) %> 
<% if @kassi_event.eventable_type.eql?("Listing") %>
<%= other_party.name %> on merkinnyt sinut ilmoituksessa "<%= @kassi_event.eventable.title %>" mainitun pyynnön toteuttajaksi.
<% else %>
<%= other_party.name %> on kiittänyt sinua palveluksesta "<%= @kassi_event.eventable.title %>".
<% end %>

Voit nähdä kassitapahtuman tiedot ja kommentoida käyttäjän <%= other_party.name %> toimintaa osoitteessa <%= @url %>

<%= render :partial => "mail_signature" %>