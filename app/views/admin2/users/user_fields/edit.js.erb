var popup = $('#userFieldsEditModal');
if (popup.length) {
    popup.remove();
}

$('body').append('<%= j render 'admin2/users/user_fields/edit_popup', service: @service %>');
$('#userFieldsEditModal').modal('show');
initValidate('<%= I18n.locale %>', '#submit_edit_user_field');
