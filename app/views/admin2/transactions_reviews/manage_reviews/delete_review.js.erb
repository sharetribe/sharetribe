var popup = $('#reviewDeleteBlock');
if (popup.length) {
    popup.remove();
}

$('body').append('<%= j render 'admin2/transactions_reviews/manage_reviews/delete_popup', tx: @tx %>');
$('#reviewDeleteBlock').modal('show');
