.form-group
  %label
    = t('admin2.search.search_placeholder')
  %small.form-text-above
    = t('admin2.search.search_placeholder_info_text')
  - customizations.each do |customization|
    = form.fields_for :community_customizations, customization do |cc|
      .d-flex.multiple-languages
        %span.language
          = t("admin.communities.available_languages.#{cc.object.locale}")
        = cc.text_field :search_placeholder, class: %i[form-control search-placeholder], placeholder: t('admin2.search.search_for')
