%a{:href => path_for_status_link(status, conversation, @current_user), :class => "status-link #{status}", :data => { :method => "put", :remote => "true"}}
  .icon-with-text{:class => icon_for("#{status}ed")}
  .text-with-icon= t(".#{status}_#{conversation.discussion_type}")
