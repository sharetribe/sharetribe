<h1><%= t(:borrow) + " " + @item.title.downcase + " " + t(:from_user) + " " + @person.name(session[:cookie]) %></h1>

<div id="basic_content">

<%= render :partial => "messages/send_message" %>

</div>
