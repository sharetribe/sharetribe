.profile-badge
  = link_to image_tag("https://s3.amazonaws.com/sharetribe/assets/images/badges/#{badge.name}_medium.png", :alt => t(".#{badge.name}"), :title => t(".#{badge.name}")), "#", :id => "#{badge.name}_description_link"
