- if current_user?(listing.author)
  - if listing.closed?
    = link_to t("listings.edit_links.reopen_listing"), edit_listing_path(:id => listing.id)
  - else
    = link_to t("listings.listed_listing.edit"), edit_listing_path(:id => listing.id)
    &nbsp;|&nbsp;
    = link_to t("listings.edit_links.close_listing"), close_person_listing_path(@current_user, listing), :method => :put, :remote => true
