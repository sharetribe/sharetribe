<h1><%= t(:add_new_listing) %></h1>

<div id="breadcrumb">
<h3>
<% if params[:category] %>
  <% if params[:subcategory] %>
    <%= link_to(t(params[:category]), "/listings/new") + " > " + link_to(t(params[:subcategory]), "/listings/new?category=" + params[:category]) + " > " + t(:listing_text)  %>
  <% else %>
    <% if (Listing.get_sub_categories(params[:category])) %>
      <%= link_to(t(params[:category]), "/listings/new") + " > " + t(:select_sub_category) %>
    <% else %>
      <%= link_to(t(params[:category]), "/listings/new") + " > " + t(:listing_text) %>
    <% end %>  
  <% end %>
<% else %>
  <%= t(:select_category) %>
<% end %> 
</h3>
</div>