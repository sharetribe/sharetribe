-# if @listings.size > 0
-#  = render :partial => "listings/listed_listing", :collection => @listings, :as => :listing, :spacer_template => "listings/listing_spacer"
-#  = pageless(@listings.total_pages, '#search_results', @request_path, t(".loading_more_#{@title}s"))
-#- else
-#  #no_listings_found
-#   - if params[:listing_type]
-#      = t(".no_#{params[:listing_type]}s_with_the_conditions_you_gave")
-#      = link_to t(".#{Listing.opposite_type(params[:listing_type])}_something"), new_request_path(:type => Listing.opposite_type(params[:listing_type]))

-# Key for localhost: &amp;key=ABQIAAAA-k4rTPisb4nHvJoXjsnoKhT2yXp_ZAY8_ufC3CFXhHIE1NvwkxTuQFJ_mPOs2Qq1IAfYilnGH0zLFg

-# With Loader
- content_for :extra_javascript do
  = javascript_include_tag "http://www.google.com/jsapi"

- content_for :extra_javascript do
  :javascript
    google.load("maps", "3", {"other_params":"sensor=true"}, {callback: initialize});

    function initialize() {
      var mapDiv = document.getElementById('map-canvas');

      var latlng = new google.maps.LatLng(37.4419, -100.1419);

      if (google.loader.ClientLocation) {
        latlng = new google.maps.LatLng(google.loader.ClientLocation.latitude, google.loader.ClientLocation.longitude);
      }       
      var map = new google.maps.Map(mapDiv, {
        center: latlng,
        zoom: 13,
        mapTypeId: google.maps.MapTypeId.ROADMAP
      });
    }

#map-canvas{:style => "width: 100%; height: 400px"}

-# Without Loader
- content_for :extra_javascript do
  = javascript_include_tag "http://maps.google.com/maps/api/js?sensor=true"

- content_for :extra_javascript do
  :javascript
    function initialize() {
      var mapDiv = document.getElementById('map-canvas');
      var lat = 37.4419;
      var lng = -122.1419;
      
      var map = new google.maps.Map(mapDiv, {
        center: new google.maps.LatLng(lat, lng),
        zoom: 13,
        mapTypeId: google.maps.MapTypeId.ROADMAP
      });
    }
    google.maps.event.addDomListener(window, 'load', initialize);

#map-canvas{:style => "width: 100%; height: 400px"}

